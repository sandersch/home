# Camera segment is IPv4-only (build-plan.md Phase 1.1). Disabling IPv6 on NIC2 removes
# the ICMPv6/router-discovery surface entirely rather than relying on it falling through
# to the nftables drop. Apply with `sudo sysctl --system`.
net.ipv6.conf.enp87s0.disable_ipv6 = 1
