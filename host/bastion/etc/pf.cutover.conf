# Temporary deny-all policy used only during the attended interface cutover.
# It deliberately has no dependency on VLAN interface existence or addressing.
set block-policy drop
set skip on lo
block all
