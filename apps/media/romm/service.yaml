apiVersion: v1
kind: Service
metadata:
  name: romm
  namespace: media
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: romm
  ports:
    - name: http
      port: 8080
      targetPort: http
