# Media

Media workload manifests will live here: Plex, Gluetun, SABnzbd, the `*arr` stack,
Overseerr, and RomM.

Stateful app config should use `local-nvme`; high-write scratch data should use
`topolvm-scratch`.
