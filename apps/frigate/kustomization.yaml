apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: frigate

resources:
- namespace.yaml
- storage.yaml
- service.yaml
- ingress.yaml
- deployment.yaml
- frigate.sops.yaml
- frigate-mqtt.sops.yaml

configMapGenerator:
- files:
  - config.yml
  name: frigate-config
